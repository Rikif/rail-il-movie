import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddMovieForm';

export default defineMessages({
  header_text: {
    id: `${scope}.hedaer`,
    defaultMessage: 'movies app',
  },
  movies_list: {
    id: `${scope}.hedaer`,
    defaultMessage: 'go to list',
  }
});